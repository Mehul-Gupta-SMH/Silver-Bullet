# SilverBullet Frontend — environment variable template
# Copy this file to .env.local for local development.

# API base URL. In development, Vite proxies /api to the backend automatically.
# For production builds pointing at a separate backend, set this to the absolute URL.
VITE_API_URL=/api/v1
